name: Deploy theme over FTP

# Тема на хостинге состоит из двух частей: PHP-шаблоны лежат в git, а CSS/JS
# собираются Vite в wp-theme/build/ и в git не попадают (.gitignore). Поэтому
# заливать wp-theme/ можно только после сборки — иначе на сервер уедут шаблоны
# со старыми стилями
on:
  push:
    branches: [main]
    paths:
      - "wp-theme/**"
      - "src/**"
      - "package.json"
      - "package-lock.json"
      - "vite.theme.config.js"
      - ".github/workflows/deploy-theme.yml"
  workflow_dispatch:

permissions:
  contents: read

# Параллельные заливки затирают друг другу файл состояния синхронизации на
# сервере, поэтому прогоны идут строго по одному. cancel-in-progress: false —
# оборванная на середине заливка оставила бы тему в несогласованном виде
concurrency:
  group: ftp-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check FTP settings
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
          FTP_SERVER_DIR: ${{ vars.FTP_SERVER_DIR }}
        # Без этой проверки отсутствующий секрет всплыл бы только на шаге
        # заливки — уже после сборки и с невнятной ошибкой подключения.
        # У FTP_SERVER_DIR намеренно нет значения по умолчанию: неверный путь
        # тихо создал бы дерево файлов мимо WordPress, в корне FTP-аккаунта
        run: |
          missing=""
          if [ -z "$FTP_SERVER" ]; then missing="$missing FTP_SERVER (secret)"; fi
          if [ -z "$FTP_USERNAME" ]; then missing="$missing FTP_USERNAME (secret)"; fi
          if [ -z "$FTP_PASSWORD" ]; then missing="$missing FTP_PASSWORD (secret)"; fi
          if [ -z "$FTP_SERVER_DIR" ]; then missing="$missing FTP_SERVER_DIR (variable)"; fi
          if [ -n "$missing" ]; then
            echo "::error::Не заданы:$missing — Settings → Secrets and variables → Actions"
            echo "::error::FTP_SERVER_DIR — путь к теме со слешем на конце, обычно /public_html/wp-content/themes/wh/"
            exit 1
          fi
          case "$FTP_SERVER_DIR" in
            */) ;;
            *) echo "::error::FTP_SERVER_DIR должен заканчиваться слешем: $FTP_SERVER_DIR"; exit 1 ;;
          esac

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build theme assets
        run: npm run build:theme

      # Заливаются только изменившиеся файлы: экшен хранит на сервере
      # .ftp-deploy-sync-state.json с хешами. На первом прогоне файла нет,
      # поэтому уедет вся папка целиком — это нормально
      - name: Upload theme over FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # ftps — шифрованный FTP на том же 21 порту. Если хостинг его не
          # поддерживает, задайте переменную FTP_PROTOCOL = ftp
          protocol: ${{ vars.FTP_PROTOCOL || 'ftps' }}
          local-dir: ./wp-theme/
          server-dir: ${{ vars.FTP_SERVER_DIR }}
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
